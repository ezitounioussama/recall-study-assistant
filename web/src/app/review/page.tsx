import { NotBuiltYet } from "@/components/ui/not-built";

export default function ReviewPage() {
  return (
    <NotBuiltYet
      category="Review"
      pr="PR 6"
      headline="One card at a time, on an FSRS schedule."
      what="Rate a card and its stability, difficulty and retrievability all update. The next interval is computed from that memory model, not picked from a fixed ladder."
    />
  );
}
