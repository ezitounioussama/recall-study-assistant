// Tailwind v4 is configured in CSS, not JS — the theme lives in
// src/app/globals.css under @theme, mapped one-to-one from
// docs/design-language.md. There is no tailwind.config.js on purpose.
export default { plugins: { "@tailwindcss/postcss": {} } };
