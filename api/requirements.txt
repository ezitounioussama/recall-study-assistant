fastapi>=0.120
uvicorn[standard]>=0.35
pydantic[email]>=2.10   # EmailStr needs the email-validator extra
pydantic-settings>=2.7
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
# argon2id, the current password-hashing recommendation. Not bcrypt: bcrypt
# silently truncates at 72 bytes, which turns a long passphrase into a shorter
# one without telling anyone.
argon2-cffi>=23.1
itsdangerous>=2.2      # signs the session cookie
python-multipart>=0.0.20
pytest>=8.3
pytest-asyncio>=0.25
httpx>=0.28
pypdf>=5.1               # PDF text extraction; pure python, no system deps
numpy>=2.0               # cosine similarity over stored embeddings
